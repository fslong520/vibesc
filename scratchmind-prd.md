# 智搭（ScratchMind）PRD

## AI 速读卡

```
产品名：智搭（ScratchMind）
一句话：全模态 AI 驱动的可视化编程桌面客户端——说话生成积木，描述生成角色，对话生成动画
目标用户：6-14 岁编程初学者、编程教育机构教师
核心场景：孩子说"让猫追老鼠"，AI 自动生成积木、画出小猫、做出走路动画
平台：桌面客户端（Tauri / Electron），Windows / macOS / Linux
关键差异：Agnes 全模态 AI（Chat+Image+Video）统一驱动 + 原生硬件通信（串口/蓝牙/HID）+ 本地插件目录
P0功能：积木编辑器 + AI 对话面板 + NL→积木生成 + 内置硬件积木 + 本地项目文件系统
技术栈：Tauri (Rust) + TurboWarp (scratch-vm 编译器优化版) + Web Worker + Agnes AI 多模态引擎
风险项：Agnes 免费 API 速率限制与稳定性、儿童安全过滤、Tauri 的 Web Worker 沙箱与原生桥接安全性
商业化：免费增值——基础 AI 调用免费，高频使用/视频生成需充值 Agnes credits
```

---

## 第一章：产品概述

### 1.1 产品定义

智搭是一个桌面客户端形态的大模型驱动的可视化编程平台，面向 6-14 岁儿童。核心编辑器基于 **TurboWarp**（Scratch 的高性能编译器优化版），运行流畅度远超原生 Scratch。在此基础上嵌入全模态 AI 编程搭档——孩子用自然语言说出想法，AI 将其转化为积木脚本；说"画一只红色小猫"，AI 直接生成角色图片和造型动画；遇到问题时，AI 用积木语义而非抽象代码概念来解释。

**产品等式**：智搭 = 桌面 Scratch 编辑器 + 全模态 AI 搭档（Agnes）+ 自然语言→积木编译器 + 智能编程老师 + 素材生成器（角色/背景/动画）+ 本地硬件扩展引擎

**为何桌面客户端**：
- **原生硬件通信**：串口（Serial Port）、蓝牙（Bluetooth HID/SPP）、USB HID——浏览器 Web Serial API 仅 Chrome/Edge 支持，且功能受限。桌面客户端可无障碍操作硬件
- **本地文件系统**：插件直接存放在 `~/.scratchmind/plugins/` 目录，用户可在文件管理器里管理、编辑、分享插件文件
- **离线可用**：核心编辑器完全离线运行，仅 AI 调用需联网。网络不佳时编辑器不瘫痪
- **系统集成**：可注册为默认 `.sb3` 和 `.zhplugin` 文件打开方式，双击文件直接编辑
- **性能**：scratch-vm 渲染、视频截帧、大文件处理不受浏览器内存限制

### 1.2 目标用户画像

| 画像 | 描述 | 核心痛点 |
|------|------|----------|
| 编程初学者（6-10 岁） | 刚接触编程，拖积木都嫌抽象 | 不知道用什么积木、怎么组合 |
| 进阶学习者（10-14 岁） | 会基础积木但做不出完整项目 | 卡在逻辑组合和调试上 |
| 编程教师 | 用 Scratch 授课 | 备课耗时，难以及时回答每个学生的问题 |

**硬约束**：产品必须面向儿童，全中文界面，所有文案符合 6 岁儿童阅读理解水平。

### 1.3 可行性边界

**推荐默认**：
- 桌面客户端基于 **Tauri**（Rust 后端 + 系统 WebView 渲染），比 Electron 更小（安装包 < 20MB）、更快、内存占用更低
- AI 后端使用 **Agnes AI**（免费全模态 API），统一处理三类任务：
  - **Chat**（`agnes-2.0-flash`）：自然语言→积木脚本、对话式调试、积木解说
  - **Image**（`agnes-image-2.0-flash`）：角色/背景/造型生成，白底或透明背景输出
  - **Video**（`agnes-video-v2.0`）：角色动画、造型变化动画（如"小猫从站到跳"）
- 插件存储在本地文件系统 `~/.scratchmind/plugins/`，重启自动加载
- 项目文件存储在本地 `~/.scratchmind/projects/`，支持导出 `.sb3`
- 无需注册即可使用全部本地功能，AI 功能需配置 Agnes API Key

**硬约束**：
- **所有 AI 生成内容必须经过安全过滤层**（不能生成暴力、色情、不适内容），这是儿童产品的生命线
- 安全过滤分三层：输入文本过滤 + 输出文本过滤 + 生成图片/视频内容审核
- 每次 AI 调用必须有 loading 状态和超时处理，不得让儿童无反馈等待
- 生成的积木必须语法正确，不允许生成不完整的结构（如 if 没有 then）
- 不支持用户自定义 prompt——AI 对儿童是黑盒
- 图片生成默认白色背景或透明背景，不得生成带文字的图片
- 插件 `~/.scratchmind/plugins/` 目录不得包含可执行文件（仅允许 .json/.js），防止恶意插件

**发挥空间**：
- 是否增加语音输入（调用系统麦克风 API，桌面端无浏览器权限限制）
- 是否支持 macOS / Windows / Linux 三端分别打包
- 是否集成系统托盘图标，显示硬件连接状态和 AI 余额

### 1.4 硬件扩展定位

智搭的硬件扩展参考 micro:bit 的设计哲学——**低门槛、高上限**：

- **低门槛**：内置常用硬件积木块（LED 控制、按键读取、舵机、蜂鸣器、温湿度传感器），拖拽即用，无需写代码
- **高上限**：用户可编写自己的硬件插件（JavaScript/TypeScript），定义专属积木块，控制任意硬件
- **通信方式**：
  - 原生 **Serial Port**（通过 Rust 后端 `serialport` crate）——真正串口通信，不受浏览器限制
  - 原生 **Bluetooth SPP / BLE**（通过 Rust 后端 `btleplug` 或系统蓝牙 API）
  - WebSocket（网络硬件）
- **典型硬件**：micro:bit、Arduino、ESP32、行空板、树莓派 Pico——只要硬件通过串口/蓝牙收发 JSON 指令即可
- **桌面优势**：无需安装 Chrome 或 Edge，不需要 Web Serial 这种浏览器专有 API。串口通信是操作系统原生的

**硬约束**：
- 所有硬件插件运行在 Web Worker 沙箱中，通过 Tauri 命令桥接（invoke）与 Rust 后端通信——插件不能直接访问串口 API
- 插件代码不能发起网络请求，所有 IO 必须通过 Tauri invoke 转发
- 硬件通信必须用户主动触发（连接操作），不得自动连接
- 每个插件必须在 manifest 中声明所需权限（serial/bluetooth/network）
- 串口通信在 Rust 后端用 `serialport` crate 实现，通过 IPC 暴露给前端 Web Worker

**发挥空间**：
- 官方维护常见硬件的插件库（micro:bit、Arduino UNO、ESP32）
- 社区插件市场，用户上传/下载插件
- 可视化插件编辑器——拖拽配置硬件引脚，AI 辅助生成插件代码

### 1.5 商业模式

智搭采用**免费增值（Freemium）+ Agnes Credits 充值引导**模式。

**免费层（零成本可用）**：
- 积木编辑器全部功能免费
- 内置硬件积木免费
- AI 对话生成积木：每人每天 20 次免费调用
- AI 角色/背景生成：每人每天 5 次免费生成
- AI 视频生成：每人每天 1 次免费生成

**付费层（Agnes Credits 充值）**：
- 超出免费额度后，引导用户购买 Agnes Credits 充值包
- 充值包示例：
  - 小包：¥9.9 / 100 Credits（约 50 次 AI 调用）
  - 中包：¥29.9 / 500 Credits（送 10 次视频生成）
  - 大包：¥99.9 / 2000 Credits（送 50 次视频生成 + 优先队列）
- 每次 AI 调用消耗 Credits：
  - Chat 生成积木：2 Credits/次
  - 图片生成：10 Credits/次
  - 视频生成：30 Credits/次

**充值引导策略（硬约束）**：
- 免费额度用完后，不阻断使用——弹出"今日免费次数用完啦，充值继续玩？"底部浮层，可关闭
- 关闭后当天可继续使用（体验降级——响应速度降为慢速队列），第二天刷新免费额度
- 充值入口在三个位置：AI 对话面板顶部余额显示、设置页、免费额度耗尽弹窗
- 不得在儿童操作路径中直接要求付费——付费决策必须由家长完成
- 充值使用 Agnes 官方支付通道，平台不代收——用户购买的是 Agnes Credits，而非智搭会员

**推荐默认**：
- Credits 消耗系数需上线后根据 Agnes API 实际成本调整
- 教育机构批量采购可议价（统一开 Agnes 企业账号）
- MVP 阶段免费额度放宽（Chat 50 次/天），先积累用户再收紧

## 第二章：整体布局与导航

### 2.1 页面结构

布局核心原则：**主区零挤压**——Snap! 标准三栏完全保留，所有 AI/素材/硬件/插件功能放在底部可折叠面板和独立弹窗中。

```
┌──────────────────────────────────────────────────────────────┐
│  顶部导航栏                                    [AI 🪙 18/20]│
│  [🔲 智搭 Logo] [创建] [打开] [导出] [教程] [用户名 ▼]    │
├──────────┬──────────────────────────┬───────────────────────┤
│ 积木区    │  脚本编辑区              │  舞台区               │
│          │                          │                       │
│ 运动      │      [积木拖拽区域]      │  ┌───────┐            │
│ 外观      │                          │  │ ▶     │            │
│ 声音      │                          │  └───────┘            │
│ 事件      │                          │                       │
│ 控制      │                          │  角色列表              │
│ 侦测      │                          │  [🐱小猫]             │
│ 运算      │                          │  [🐶小狗]             │
│ 变量      │                          │                       │
│ ──────    │                          │  [背景] [音效]        │
│ 🔧硬件    │                          │                       │
│ (插件扩展) │                          │                       │
├──────────┴──────────────────────────┴───────────────────────┤
│  ◉ AI 对话  │  ○ 素材工坊  │  ○ 硬件  │  ○ 插件  │   ▲    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  [🤖] 你好！我是你的编程老师，想做什么项目呀？                │
│                                                              │
│  [👧] 让小猫走10步                                [发送]    │
│                                                              │
│  [🤖] 好啦！我帮你做了这些积木：                              │
│       ✓ 当绿旗被点击                                         │
│       ✓ 移动10步                            [插入到编辑器]   │
│                                                              │
│  [👧] 画一只红色小猫                                         │
│  [🤖] 正在用 AI 画画... 🎨                       [进度条]   │
│       ┌──────────────────────┐                               │
│       │  (图片预览)          │  [导入为角色] [去素材工坊]    │
│       └──────────────────────┘                               │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  状态栏：项目"未命名"  │  角色: 小猫  │  积木: 5  │  60FPS  │
└──────────────────────────────────────────────────────────────┘
```

**布局分层**：

| 层 | 位置 | 内容 | 交互 |
|------|------|------|------|
| 主编辑区 | 中央，固定 | 积木区 + 脚本编辑区 + 舞台（标准 Snap! 三栏） | **永不变化**，不被其他面板挤压 |
| 底部面板 | 底部，可折叠 | 四个标签页：AI 对话 / 素材工坊 / 硬件 / 插件 | 点击标签切换，拖拽调整高度（100-400px），可完全折叠 |
| 素材工坊（全屏） | 独立弹窗 | 图片切割、去背景、AI 重绘、造型序列编辑 | 模式窗口，编辑完关掉回到主区 |
| 顶部栏 | 顶部，固定 | Logo + 菜单 + AI 余额 | 轻量信息展示 |

### 2.2 底部面板切换逻辑

```
┌────────────────────────────────────────────┐
│  ◉ AI 对话  │  ○ 素材工坊  │  ○ 硬件  │  ○ 插件  │
├────────────────────────────────────────────┤
│  当前标签页内容填充整个底部面板区域          │
│                                            │
│  - AI 对话：聊天 + 积木/图片/视频结果      │
│  - 素材工坊：切割工具 + 去背景 + AI 重绘   │
│  - 硬件：串口连接 + 状态监视               │
│  - 插件：已安装列表 + 安装/卸载管理        │
└────────────────────────────────────────────┘
```

- 底部面板默认**折叠**（只显示标签栏），减少对孩子注意力的干扰
- 点击标签展开对应面板，再次点击同一标签折叠
- 拖拽面板上边缘调整高度，最低 100px，最高 400px
- 当底部面板展开时，主编辑区高度自适应缩减
- 所有标签页保留各自状态（切换不丢失输入/结果）

### 2.3 导航逻辑

**推荐默认**：单窗口桌面应用，所有操作在一个窗口内完成。

1. **主编辑区**（永远可见）：
   - 积木区：左侧，标准 Snap! 积木分类 + 硬件扩展分类
   - 脚本编辑区：中间，积木拖拽与组合
   - 舞台区：右侧，项目预览 + 角色/背景管理

2. **底部面板标签页**：
   - **AI 对话**：最常用，默认激活。孩子在此说话，AI 返回积木/图片/视频结果
   - **素材工坊**：编辑角色造型（切割、去背景、重绘）
   - **硬件**：串口连接、设备状态、通信日志
   - **插件**：安装/卸载/管理用户插件

3. **模式弹窗**：
   - 素材工坊全屏编辑
   - 项目设置（名称、描述）
   - 家长/老师管理（API Key 配置、Credits 充值）

**硬约束**：
- 底部面板展开时不得遮挡舞台右下角的绿旗/停止按钮
- "插入到编辑器"按钮必须在 AI 对话标签页中显眼位置，一个点击完成

---

## 第三章：核心模块详细设计

### 3.1 积木编辑器

**推荐默认**：基于 **TurboWarp**（scratch-vm 的 JIT 编译优化版），相比原生 Scratch：
- 脚本执行速度提升 10-100x（尤其适合复杂游戏和硬件循环控制）
- 支持 60FPS 渲染（原生 Scratch 仅 30FPS），动画更丝滑
- 编译器将积木直接编译为 JavaScript，而非逐块解释执行
- 完全兼容 Scratch 3.0 的 `.sb3` 格式和积木体系
- 内置扩展系统可自定义积木块——插件无需修改 scratch-vm 源码

```
积木编辑器状态机:

[空闲] ──拖拽积木──→ [编辑中] ──撤销/重做──→ [编辑中]
  ↑                     │
  │                     ├── 播放按钮 ──→ [运行中] ──停止──→ [空闲]
  │                     │
  └── AI 生成积木 ──────┘
```

**正常流程**：
1. 孩子从左侧积木区拖拽积木到脚本编辑区
2. 积木自动吸附对齐
3. 孩子点击运行，舞台执行脚本
4. 孩子可以随时拖拽调整或删除积木

**失败路径 1 — 积木冲突**：
- 场景：孩子将"移动10步"积木错误插入到"重复执行"的条件槽（语法不允许）
- 行为：编辑器拒绝吸附，积木弹回原位置，AI 面板提示"这个积木不能放在这里哦"
- 状态：编辑器保持编辑中状态

**失败路径 2 — VM 运行时错误**：
- 场景：脚本包含死循环
- 行为：scratch-vm 检测脚本运行超时（5 秒），自动终止并提示
- 状态：[运行中] → [空闲]，舞台停止，AI 面板显示"脚本运行太久，已自动停止"

**硬约束**：
- 积木拖拽必须 60fps 响应
- 所有积木操作必须支持撤销/重做（至少 50 步历史）
- 必须兼容 `.sb3` 格式的导入导出
- 积木区文字标签用大号字体（16px+），方便儿童阅读

### 3.2 AI 对话面板

这是产品的核心差异化模块。面向儿童设计，语言亲切活泼。

```
┌──────────────────────────────┐
│ AI 编程老师    余额: 18/20 🪙│
├──────────────────────────────┤
│                              │
│ [🤖] 你好！我是你的编程老师。│
│      你想做什么项目呀？       │
│                              │
│ [👧] 让小猫追着鼠标跑        │
│       ┌────────────────┐    │
│       │ [生成积木]      │    │
│       └────────────────┘    │
│                              │
│ [🤖] 好啦，我帮你做好了！   │
│      ✓ 当绿旗被点击           │
│      ✓ 重复执行               │
│      ✓   面向鼠标指针         │
│      ✓   移动10步             │
│      ✓   等待0.1秒            │
│                              │
│  ┌────────────────────────┐  │
│  │ [插入到编辑器] [不满意]│  │
│  └────────────────────────┘  │
│                              │
│ [👧] 太慢了                   │
│ [🤖] 没问题！把"移动10步"     │
│      改成25步，"等待0.1秒"    │
│      去掉，就跑快了！         │
│                              │
├──────────────────────────────┤
│ [输入框...             ] [→] │
│ 🎤                         │
└──────────────────────────────┘
```

**正常流程（NL→积木）**：
1. 孩子在输入框输入中文描述（或点击🎤语音输入）
2. 点击发送或按 Enter
3. 对话面板显示可爱的 loading 动画（小机器人思考）
4. AI 分析自然语言，识别意图（动作、触发条件、循环结构等）
5. AI 返回一组结构化积木定义
6. 对话面板展示积木描述列表（每行一个带图标的中文说明）
7. 孩子点击"插入到编辑器"，积木自动出现在脚本编辑区
8. 孩子可继续说话修改

**正常流程（AI 调试）**：
1. 孩子描述问题："点击绿旗后小猫不动"
2. AI 读取当前项目所有积木和角色状态
3. AI 返回诊断+修复建议，用儿童能懂的语言解释
4. 孩子可选择"帮我修好"

**失败路径 1 — LLM 超时/失败**：
- 场景：网络问题或 LLM API 返回错误
- 行为：超时 10 秒后显示"网络开小差了，待会再试试吧"
- 降级方案：展示预设的常见积木模板列表（完全离线可用）
- 状态：对话面板显示错误状态，编辑器不受影响

**失败路径 2 — LLM 生成无效积木**：
- 场景：LLM 返回的积木指令语法错误
- 行为：后端验证层拦截，不返回给前端
- 行为：自动切换 LLM 后端重试（若配置了多个后端）或同后端重试（最多 2 次）
- 最终降级：回复用户"这个想法我还没学会，换个说法试试？"

**失败路径 3 — 安全过滤拦截**：
- 场景：孩子输入"做爆炸效果"或 LLM 返回了不当内容
- 行为：安全过滤器拦截，对话面板提示"这个话题不太合适哦，换个点子吧"
- 状态：对话保持，编辑器不受影响
- 后台：记录触发安全过滤的内容（供家长/老师审查）

**状态定义**：

| 状态 | 描述 | 儿童可见 |
|------|------|----------|
| `idle` | 等待输入 | 正常对话框 + 示例提示 |
| `thinking` | LLM 处理中 | 小机器人动效 + "正在想..." |
| `result_ready` | LLM 返回结果 | 显示积木列表 + [插入] 按钮 |
| `error` | 请求失败 | 云朵哭脸 + 重试按钮 |
| `out_of_credits` | 免费额度用尽 | 底部浮层"今天用完了，明天再来吧" + 家长充值入口 |
| `generating_image` | 生成图片中 | 画板动效 + "正在画..." |
| `generating_video` | 生成视频中 | 摄像机动效 + "正在拍..." + 进度条 |

**硬约束**：
- 每次 LLM 调用必须携带当前项目的完整积木上下文
- 对话上下文保留最近 50 轮（session 存储）
- 安全过滤器必须在 500ms 内完成判定
- AI 回复语言必须符合 6-10 岁儿童认知水平——不用"循环""变量""参数"等术语，用"重复做""小盒子""设置"等

### 3.3 舞台与预览引擎

基于 TurboWarp 编译器运行时。相比 scratch-vm，TurboWarp 将积木编译为 JavaScript 后运行，性能提升显著，尤其适合：
- 硬件循环控制（高速读取传感器数据）
- 复杂物理效果（多角色碰撞检测）
- 高帧率动画（60FPS 造型切换）

```
舞台状态机:

[停止] ──点击绿旗──→ [运行中] ──脚本执行完毕──→ [停止]
  ↑                     │  ↑
  │                     │  └── AI 生成积木后自动预览 ──→ [预览中]
  │                     │
  └── AI 预览结束 ──────┘
```

**推荐默认**：增加"AI 自动预览"模式——当 AI 生成积木并插入后，自动运行一次，让儿童立刻看到效果。

**硬约束**：
- 舞台渲染帧率默认 60FPS（TurboWarp 编译器模式），可降级到 30FPS（解释器模式）
- 预览模式最长 10 秒自动停止
- 编译器模式 / 解释器模式可由用户切换（高级设置中），默认编译器模式

### 3.4 作品集市

```
┌────────────────────────────────────────────┐
│ 大家的作品                                │
│                                            │
│ [搜索...                       ] [🔍]     │
│                                            │
│ [热门] [最新] [游戏] [动画] [故事]        │
│                                            │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐     │
│ │ 猫狗  │ │ 弹球  │ │ 时钟  │ │ 迷宫  │     │
│ │ 大战  │ │  游戏 │ │      │ │      │     │
│ │ ⭐4.5 │ │ ⭐4.8 │ │ ⭐4.2 │ │ ⭐4.6 │     │
│ └──────┘ └──────┘ └──────┘ └──────┘     │
│                                            │
│ [加载更多 →]                                │
└────────────────────────────────────────────┘
```

**推荐默认**：项目卡片显示封面、标题、作者、点赞数。儿童友好界面。

### 3.5 角色与背景管理

**硬约束**：内置 Scratch 官方角色库全部素材，支持本地上传图片。

**发挥空间**：增加"AI 画角色"——孩子描述"一只蓝色戴帽子的猫"，AI 调用绘图 API 生成新角色。

### 3.6 硬件扩展模块 ⭐

硬件扩展是智搭的差异化能力，灵感来自 micro:bit。积木区底部有独立的"硬件"分类，安装插件后自动扩展积木块。

```
硬件模块状态机:

[未连接] ──用户点击"连接设备"──→ [连接中] ──成功──→ [已连接] ──运行积木──→ [硬件运行中]
    ↑                            │                   │                    │
    │                            └──失败──→ [未连接]  │                    │
    │                                               ├── 设备断开 ──→ [未连接]
    │                                               │
    └─────── 用户断开 ───────────────────────────────┘
```

#### 3.6.1 内置硬件积木（P0）

编辑器默认提供以下硬件积木块，无需安装插件即可使用：

| 积木 | 说明 | 适用通信方式 |
|------|------|------------|
| `[当 按键 A ▼ 被按下时]` | 事件积木，等待硬件按键触发 | Serial / Bluetooth |
| `[设置 LED 引脚 0 ▼ 为 高电平 ▼]` | 设置数字引脚高低电平 | Serial |
| `[设置 舵机 引脚 9 ▼ 角度为 90]` | PWM 舵机控制 | Serial |
| `[蜂鸣器 引脚 8 ▼ 响 1 秒]` | 有源蜂鸣器控制 | Serial |
| `[读取 传感器 引脚 A0 ▼]` | 模拟值读取 → 返回 0-1023 | Serial |
| `[当 收到串口数据 (data)]` | 事件积木，接收硬件主动发来的数据 | Serial |
| `[发送串口消息 "hello"]` | 向硬件发送文本指令 | Serial |

这些内置积木使用智搭通用硬件协议——硬件通过串口/蓝牙收发 JSON 格式指令：
```json
// 智搭 → 硬件
{ "cmd": "digitalWrite", "pin": 13, "value": 1 }
{ "cmd": "servoWrite", "pin": 9, "angle": 90 }
{ "cmd": "tone", "pin": 8, "frequency": 440, "duration": 1000 }

// 硬件 → 智搭（事件）
{ "event": "buttonPressed", "pin": 0, "value": 1 }
{ "event": "sensorRead", "pin": "A0", "value": 512 }
```

**硬件固件参考**：智搭提供参考固件（Arduino/micro:bit/ESP32），实现上述协议的解析。用户烧录后即可与智搭开箱即用。

#### 3.6.2 用户插件系统（P1）

用户可编写自己的硬件插件，安装后变成新的积木类别和积木块。插件一经安装，永久保存在系统目录中，下次打开项目时自动加载，无需重复上传。

```
插件文件结构：
my-hardware-plugin/
├── manifest.json     # 插件元数据 + 权限声明
├── blocks.js         # 积木定义（积木外观 + 运行时逻辑）
└── runtime.js        # 硬件通信逻辑（Web Worker 沙箱）

// manifest.json 示例
{
  "id": "my-robot-car",
  "name": "智能小车",
  "version": "1.0.0",
  "description": "控制双轮智能小车",
  "author": "小明老师",
  "permissions": ["serial", "bluetooth"],
  "blocks": [
    { "opcode": "car_forward", "text": "向前走 [STEPS] 步", "args": [{ "name": "STEPS", "type": "number" }] },
    { "opcode": "car_turn",   "text": "向 [DIR] 转弯",      "args": [{ "name": "DIR", "type": "string", "menu": ["左", "右"] }] },
    { "opcode": "car_stop",   "text": "停车" }
  ],
  "menus": {
    "DIR": ["左", "右"]
  }
}

// blocks.js 示例 — 积木运行时（在 Web Worker 沙箱中执行）
const car = {
  forward: (steps) => send({ cmd: "motor", left: 255, right: 255, duration: steps * 100 }),
  turn: (dir) => send({ cmd: "motor", left: dir === "左" ? 0 : 255, right: dir === "左" ? 255 : 0, duration: 500 }),
  stop: () => send({ cmd: "motor", left: 0, right: 0 })
};

// 注册积木处理器
registerBlocks({
  "car_forward": (args) => car.forward(args.STEPS),
  "car_turn":    (args) => car.turn(args.DIR),
  "car_stop":    ()     => car.stop()
});
```

**插件安装与自动加载流程**：

```
┌── 首次安装 ─────────────────────────────────────┐
│                                                 │
│ 用户点击"安装插件"                               │
│    │                                             │
│    ▼                                             │
│ 弹出文件选择 → 选择 .zhplugin 文件（zip 重命名）  │
│    │                                             │
│    ▼                                             │
│ 验证 manifest 格式 + 权限声明                     │
│    │                                             │
│    ├── 权限超出范围 → 提示用户确认                │
│    │                                             │
│    ▼                                             │
│ 解压插件 → 源码存入 IndexedDB（插件持久存储）      │
│ 并向服务端同步（如已登录）                         │
│    │                                             │
│    ▼                                             │
│ 通知 Plugin Manager 注册到积木分类                 │
│    │                                             │
│    ▼                                             │
│ 积木区新增 "硬件 > 智能小车" 分类，积木可用        │
│                                                 │
└─────────────────────────────────────────────────┘

┌── 每次启动自动加载 ───────────────────────────────┐
│                                                 │
│ 用户打开智搭 / 刷新页面                          │
│    │                                             │
│    ▼                                             │
│ Plugin Manager 初始化                            │
│    │                                             │
│    ├── 查询 IndexedDB → 读取已安装插件列表        │
│    │      ↓                                      │
│    │   遍历插件，enabled === true 的依次加载       │
│    │      ↓                                      │
│    │   创建 Web Worker 沙箱                       │
│    │      ↓                                      │
│    │   注册积木到 scratch-blocks                  │
│    │      ↓                                      │
│    │   积木区展示该插件的积木                      │
│    │                                             │
│    └── 无已安装插件 → 只显示内置硬件积木          │
│                                                 │
└─────────────────────────────────────────────────┘

┌── 插件卸载 ──────────────────────────────────────┐
│                                                 │
│ 用户点击"卸载插件"                               │
│    │                                             │
│    ▼                                             │
│ 确认弹窗 → "确定要删除这个插件吗？"               │
│    │                                             │
│    ├── 确认 → 从 IndexedDB 删除插件数据           │
│    │          终止对应 Worker                     │
│    │          积木区移除分类                      │
│    │                                             │
│    └── 取消 → 不做操作                           │
│                                                 │
└─────────────────────────────────────────────────┘
```

**插件存储体系**：

| 存储层 | 用途 | 持久性 | 跨设备 |
|--------|------|--------|--------|
| `~/.scratchmind/plugins/user/`（文件系统） | 用户安装的插件完整源码 | ✅ 永久存储，删除文件即卸载 | ❌ 可手动复制 |
| `~/.scratchmind/plugins/built-in/`（文件系统） | 随客户端发布的官方插件（只读） | ✅ 随更新版本变化 | ✅ 同一版本一致 |
| 服务端（可选） | 用户已安装插件列表 + 元数据（跨设备同步） | ✅ | ✅ |
| 原始 .zhplugin 文件 | 用户备份/分享 | 用户自行管理 | 通过云盘/USB |

**推荐默认**：
- MVP 将插件写入 `~/.scratchmind/plugins/user/` 目录，重启自动扫描加载
- 服务端同步为 P2（登录用户支持跨设备同步已安装插件）
- 内置插件（micro:bit 等）放在 `~/.scratchmind/plugins/built-in/`，随客户端安装发布，不可卸载
- 插件目录结构：`{plugin_id}/manifest.json` + `blocks.js` + `runtime.js`，用户可直接在文件管理器里编辑
- 删除插件目录 = 卸载插件。支持热加载——目录变化时自动刷新

**插件沙箱安全规则（硬约束）**：
- 插件 runtime 运行在 **Web Worker** 中，无权访问 DOM、document、window
- 插件不能使用 `eval()`、`Function()`、`importScripts()` 动态执行代码
- `send()` 函数只能通过硬件接口发送数据，不能发起 HTTP 请求
- `registerBlocks()` 只能在插件初始化阶段调用，运行时不可修改积木
- 插件 manifest 必须声明所需权限，运行时若超权限操作则静默失败

**失败路径 1 — 插件格式错误**：
- 场景：manifest.json 缺少必填字段或格式错误
- 行为：拒绝加载，弹窗提示"插件格式不对，请检查 manifest.json"
- 状态：积木区不变

**失败路径 2 — 插件运行时崩溃**：
- 场景：插件 runtime.js 抛出未捕获异常
- 行为：Web Worker 自动终止，积木变灰显示"已停止"
- 恢复：用户可重新加载插件

**失败路径 3 — 硬件连接断开**：
- 场景：正在运行硬件积木时串口断开
- 行为：舞台自动停止，硬件状态面板显示"❌ 已断开"，AI 面板提示"设备断开啦，检查一下连接线"
- 恢复：用户点击"重新连接"

**硬约束**：
- 同一时刻只能连接一个硬件设备
- 硬件操作积木在未连接时显示"请先连接设备"提示，不崩溃
- 所有硬件通信必须在用户手势触发下进行（点击连接按钮），浏览器安全策略要求

### 3.7 AI 素材生成模块

基于 Agnes 多模态能力（Image + Video），智搭可以在对话中直接生成角色造型、背景图片、以及造型变化动画。

#### 3.7.1 角色/背景生成

孩子说"画一只蓝色戴帽子的猫"，AI 调用 Agnes Image API 生成角色图片，一键导入 TurboWarp。

```
孩子输入 → "画一只穿红色披风的猫，白底"
    │
    ▼
AI 判断需要生成图片
    │
    ▼
调用 Agnes /v1/images/generations
  model: agnes-image-2.0-flash
  prompt: "一只穿红色披风的猫，卡通风格，白色背景，适合scratch角色"
  n: 1
  size: "512x512"
    │
    ▼
返回图片 URL
    │
    ▼
对话面板展示图片预览 + [导入为角色] [导入为背景] [不满意]
    │
    ▼
点击"导入为角色" → 图片转为 TurboWarp 角色素材（自动透明背景化）
添加到角色列表，孩子可直接拖积木控制
```

**失败路径 1 — 图片包含不当内容**：
- 行为：安全过滤拦截，不展示，"换个描述试试"
- 记录日志供家长审查

**失败路径 2 — Agnes API 超时**：
- 行为：10 秒超时提示"画得太慢啦，再试一次"
- 降级：返回到达时仍可使用的预置角色库

**硬约束**：
- 图片生成默认用白色背景，角色导入时自动去底（背景透明化处理）
- 生图 prompt 自动追加 "white background, cartoon style, kid-friendly" 后缀
- 生成的图片不可包含文字

#### 3.7.2 造型动画生成（Video）

Scratch 的造型切换是逐帧手动制作的。智搭用 Agnes Video API 自动生成造型变化动画。

```
孩子输入 → "让小猫从坐着变成跳起来"
    │
    ▼
AI 解析 → 需要生成两个造型 + 之间的过渡动画
    │
    ▼
调用 Agnes /v1/video/generations
  model: agnes-video-v2.0
  prompt: "一只卡通小猫，从坐着姿势变为跳跃姿势，纯白背景"
  duration: 2
    │
    ▼
轮询至 complete（约 30-60 秒）
    │
    ▼
返回视频 URL
    │
    ▼
视频截帧 → 提取首帧和尾帧作为两个造型
自动导入 scratch 角色的造型列表
    │
    ▼
孩子可用"切换造型"积木在动画前后切换
```

**失败路径 1 — 视频生成超时**：
- 行为：60 秒后取消任务，"拍视频太久了，换个短点的动作"
- 降级：只生成两张静态图作为造型（首尾帧），跳过动画

**失败路径 2 — 余额不足**：
- 行为：检查 Agnes Credits 余额 → 不足 → "视频生成需要更多小星星，让爸爸妈妈充一下值吧"
- 显示充值入口（家长模式）

**硬约束**：
- 视频最长 3 秒（儿童注意力 + API 成本）
- 视频生成消耗 Credits 较多，必须明确显示消耗量
- 视频截帧功能在前端完成（用 Canvas 截取视频首尾帧）

**Credits 消耗**：
| 功能 | 每次消耗 | 免费额度 |
|------|---------|---------|
| Chat 生成积木 | 2 Credits | 20 次/天 |
| 图片生成角色/背景 | 10 Credits | 5 次/天 |
| 视频生成动画 | 30 Credits | 1 次/天 |

#### 3.7.3 全模态统一对话

以上所有能力（积木生成 + 角色生成 + 视频生成）在一个对话框中完成。AI 自主判断当前请求需要什么能力。

```
[👧] 让小猫走10步
[🤖] → 判断为 积木生成 → 调用 Chat API → 显示积木列表

[👧] 画一只红色的小猫
[🤖] → 判断为 图片生成 → 调用 Image API → 显示图片 + [导入角色]

[👧] 让这只猫从坐下到跳起来
[🤖] → 判断为 视频生成 → 调用 Video API → 进度轮询 → 导入造型

[👧] 再画一个蓝色的背景
[🤖] → 判断为 图片生成 → 调用 Image API → [导入为背景]
```

**硬约束**：
- AI 必须在回复中明确告知用户当前正在调用什么能力（"正在画画""正在拍视频"）
- 视频生成因为耗时较长，允许多任务：孩子可以在等待视频时继续编辑积木
- 视频生成期间，对话面板底部显示进度条 + 剩余时间预估

### 3.8 素材工坊

AI 生成的素材往往需要微调——裁剪、去背景、调整大小、修改局部。素材工坊是底部面板的第二个标签页，提供轻量编辑工具。如需深度编辑，可展开全屏模式。

#### 3.8.1 素材工坊布局

```
┌──────────────────────────────────────────────────────────┐
│  ○ AI 对话  │  ◉ 素材工坊  │  ○ 硬件  │  ○ 插件  │  [全屏]  │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────┐  当前素材：[🐱小猫-costume1]            │
│  │ 素材列表    │  ┌────────────────────────────┐        │
│  │            │  │                            │        │
│  │ 当前角色   │  │     [图片预览区域]          │        │
│  │ [小猫]     │  │                            │        │
│  │  • 造型1   │  │                            │        │
│  │  • 造型2 ──┼──┤                            │        │
│  │  • 造型3   │  │                            │        │
│  │            │  └────────────────────────────┘        │
│  │ 舞台背景   │                                        │
│  │ [背景1]    │  ┌──────────────────────────┐          │
│  │            │  │  ✂️ 智能切割  │  🎨 AI 重绘 │          │
│  │            │  │  🫧 去背景    │  📐 裁剪    │          │
│  │            │  │  🔄 旋转      │  ↔️ 翻转    │          │
│  │            │  │  ✏️ 画笔微调  │  ↩️ 撤销    │          │
│  │            │  └──────────────────────────┘          │
│  └────────────┘                                        │
│                                                          │
│  提示：双击角色列表中的造型可直接在此打开编辑            │
└──────────────────────────────────────────────────────────┘
```

#### 3.8.2 编辑工具

| 工具 | 功能 | 实现方式 |
|------|------|---------|
| ✂️ 智能切割 | 圈选区域，自动识别主体轮廓并裁出 | Canvas + 边缘检测算法（可调用 Agnes Image API 辅助） |
| 🫧 去背景 | 自动去除图片背景，输出透明 PNG | 内置 removebg 模型（ONNX 本地运行），或 Agnes API 兜底 |
| 🎨 AI 重绘 | 选中区域，用文字描述修改（如"把猫耳朵变成圆的"） | Agnes Image API + inpainting |
| 📐 裁剪 | 手动拖拽选框裁剪 | Canvas 原生 |
| 🔄 旋转 / ↔️ 翻转 | 90°/180° 旋转，水平/垂直翻转 | Canvas transform |
| ✏️ 画笔微调 | 自由画笔，修改局部像素 | Canvas 绘图 |
| ↩️ 撤销 | 撤销上一步操作 | Canvas 状态栈 |

**正常流程**：
1. 孩子从角色列表中选中一个造型，或在 AI 对话中生成图片后点击"去素材工坊"
2. 素材工坊标签自动激活，当前素材显示在预览区
3. 选择工具编辑（如点击"去背景"）
4. 编辑结果实时预览
5. 点击"保存" → 素材覆盖原造型或另存为新造型
6. 回到脚本编辑区，新造型立即可用

**失败路径 1 — 去背景效果不佳**：
- 场景：图片主体与背景颜色相似，自动去背景残留
- 行为：提示"智能去背景没去干净，试试手动裁剪？" + 提供画笔微调入口
- 降级：用户可手动用橡皮擦工具修正

**失败路径 2 — AI 重绘超时**：
- 场景：Agnes Image API 重绘请求超时（10 秒）
- 行为：提示"AI 修改超时了，换个描述试试？"
- 状态：素材保持原样，不丢失已有编辑

**硬约束**：
- 所有编辑操作支持撤销（至少 20 步历史）
- 编辑结果自动保存到当前项目（无需用户手动保存）
- 素材工坊关闭时，若存在未保存的编辑，提示"素材还没保存，要保存吗？"
- 全屏模式与底部面板模式功能一致，仅尺寸不同
- 智能切割和去背景优先本地运行（ONNX runtime），离线可用

**发挥空间**：
- 批量编辑：同时选中多个造型，统一去背景或裁剪
- 帧动画预览：将多个造型连起来播放，预览动画效果
- 滤镜效果：添加像素化、油画、黑白等艺术滤镜
- AI 扩图：选中区域，AI 补全周边内容（如将正方形角色图扩展为宽屏背景）



## 第四章：超越竞品的差异化功能

### 4.1 核心差异

| 维度 | 传统 Scratch | 智搭 |
|------|-------------|------|
| 入门门槛 | 需理解积木分类和作用 | 说话即可开始 |
| 调试方式 | 逐块检查，需要经验 | AI 自动诊断，儿童语音解释 |
| 教学辅助 | 无原生教学能力 | AI 实时伴随教学 |
| AI 能力 | 无 | **Agnes 全模态统一驱动**：Chat 做积木、Image 画角色、Video 做动画 |
| 素材创作 | 需手动绘图或从社区找 | 说"画一只猫" AI 直接生成角色 + 造型动画 |
| 硬件扩展 | 需第三方插件（如 ScratchX） | 内建硬件积木 + 用户可编写插件（Web Worker 沙箱） |
| 费用 | 免费 | 免费增值——基础 AI 免费，高频/视频生成需充值 Credits |

### 4.2 结构性差异分析

**差异 1：自然语言作为一等输入**

传统 Scratch 的输入手段只有鼠标拖拽。智搭将自然语言提升为与拖拽并行的一等输入方式。这并非简单地在编辑器上加个 AI 聊天框——而是要求 LLM 理解 Scratch 积木的**儿童语义**（"跟随鼠标"不是翻译成 `mouseX` 坐标数值，而是生成 `面向鼠标指针` + `移动N步` 的积木组合）。这需要专门的 Prompt 工程来教 LLM 理解积木的儿童友好语义。

**差异 2：上下文感知的 AI**

智搭的 AI 不仅生成积木，还能读取当前项目中的所有角色、脚本、背景。当孩子说"让它动起来"时，AI 知道"它"指哪个角色、当前有什么积木、应该补充什么。这是通用 AI 编程助手所不具备的——教育场景的指代消解更依赖上下文。

**差异 3：安全前置而非后置**

Scratch 社区的安全主要靠人工审核和举报。智搭在 AI 生成阶段就嵌入安全过滤层——过滤输入、过滤输出、过滤运行（防止恶意脚本）。这对儿童产品是结构性差异，不是可选的加分项。

**差异 4：全模态 AI 统一驱动**

传统 Scratch 编辑器里，生成积木、画角色、做动画是三件独立的事——孩子得在不同工具间切换。智搭用 **Agnes AI** 一个后端统一了三者：
- `agnes-2.0-flash`（Chat）：理解自然语言，生成积木脚本，调试问题
- `agnes-image-2.0-flash`（Image）：看懂描述，画出角色造型和舞台背景
- `agnes-video-v2.0`（Video）：自动生成造型变化动画，截帧转为造型序列

这三个能力在一个对话框中完成。孩子说"让猫追老鼠，猫是红色的"，AI 一次性理解全部意图——先画红色小猫，再生成"面向鼠标+移动10步"的积木。这比让用户在不同工具间切换的效率差距是结构性的。

**差异 5：开放的硬件插件生态**

Scratch 官方硬件支持（micro:bit、LEGO EV3）只限于少数合作厂商，第三方开发者无法扩展。智搭的插件系统把硬件扩展能力开放给每个用户——教师可以为自己定制的传感器板写插件，机构可以封装自己的教具积木包，创客可以为自己的机器人平台做插件。

与 micro:bit 的 MakeCode 平台不同，MakeCode 的扩展是 TypeScript 编译进固件的，而智搭的插件是**运行时积木**——插件的积木块在浏览器中定义和执行，与硬件通过串口/蓝牙通信。这意味着：
- 插件的积木可以随时更新，无需重新烧录固件
- 同一硬件可以加载不同插件实现不同功能
- 插件可以用纯 JavaScript 撰写，学习成本远低于嵌入式开发

### 4.3 超预期机会

1. **AI 积木解说员**：点击积木块，AI 用儿童语言解释这个积木做了什么，用比喻而非术语——"这个积木是让小猫往前跳一步"
2. **一键项目生成**：孩子说"做一个打地鼠游戏"，AI 一次性生成完整项目（角色 + 积木 + 背景）
3. **积木飞入动画**：AI 生成积木时，积木块逐个"飞入"脚本编辑区并自动拼接，制造视觉惊喜——对孩子尤其有吸引力
4. **多解法对比**：AI 用 3 种不同方式实现同一效果，教孩子"编程有好多种方法"
5. **AI 写硬件驱动**：孩子说"我想控制一个温度传感器，当温度超过30度就亮红灯"，AI 自动生成硬件插件代码（含 manifest+blocks+runtime），用户一键加载——软件篇编程和硬件篇编程的界限被打通
6. **AI 调试硬件连接**：硬件不工作？AI 读取串口日志分析问题，"看起来是引脚接错了，试试把信号线从 D8 换到 D9"

**发挥空间**：以上标记为 P1-P2，非 MVP 必须。

---

## 第五章：数据模型

### 5.1 项目数据结构（核心）

```jsonc
{
  "version": "1.0.0",
  // ---- 元数据 ----
  "project": {
    "id": "proj_a1b2c3d4",
    "title": "猫抓老鼠",
    "description": "用上下左右键控制猫",
    "cover": "data:image/png;base64,...",
    "created_at": "2026-06-24T10:00:00Z",
    "updated_at": "2026-06-24T12:30:00Z",
    "author_id": "user_001",
    "is_public": false,
    "tags": ["游戏", "动物"],
    "complexity": "beginner"
  },
  // ---- Scratch 3.0 兼容层 ----
  "sb3_compat": {
    "vm_version": "3.0.0",
    "extensions": []
  },
  // ---- 角色 + 舞台 ----
  "targets": [
    {
      "is_stage": true,
      "name": "舞台",
      "variables": {},
      "lists": {},
      "broadcasts": {},
      "blocks": {},
      "costumes": [
        {
          "name": "背景1",
          "dataFormat": "svg",
          "assetId": "cd21514d0531fdffb22204e0ec5ed84a",
          "md5": "背景1.svg",
          "rotationCenterX": 240,
          "rotationCenterY": 180
        }
      ],
      "sounds": []
    },
    {
      "is_stage": false,
      "name": "小猫",
      "variables": {},
      "lists": {},
      "broadcasts": {},
      "blocks": {
        "block_id_001": {
          "opcode": "event_whenflagclicked",
          "next": "block_id_002",
          "parent": null,
          "inputs": {},
          "fields": {},
          "shadow": false,
          "topLevel": true,
          "x": 100,
          "y": 50
        },
        "block_id_002": {
          "opcode": "control_repeat",
          "next": null,
          "parent": "block_id_001",
          "inputs": {
            "TIMES": { "name": null, "block": "block_id_003" },
            "SUBSTACK": { "name": null, "block": "block_id_004" }
          },
          "fields": {},
          "shadow": false,
          "x": 100,
          "y": 100
        }
      },
      "costumes": [
        {
          "name": "cat1",
          "dataFormat": "svg",
          "assetId": "b7853f557e4426412e64bb3da6531a99",
          "md5": "cat1.svg",
          "rotationCenterX": 50,
          "rotationCenterY": 50
        }
      ],
      "sounds": []
    }
  ],
  // ---- AI 对话历史（智搭专属） ----
  "chat_history": [
    {
      "role": "user",
      "content": "让小猫追着鼠标跑",
      "timestamp": "2026-06-24T12:00:00Z"
    },
    {
      "role": "assistant",
      "content": "好的，我帮你做好了：...",
      "generated_blocks": ["block_id_001", "block_id_002"],
      "timestamp": "2026-06-24T12:00:05Z"
    }
  ],
  // ---- Agnes AI 配置 + Credits 余额 ----
  "agnes_config": {
    "api_key_last_four": "sk-...abcd",     // 仅存末四位用于显示
    "credits_balance": 200,                 // 实时余额（从 Agnes API 同步）
    "daily_usage": {
      "chat": 3,                            // 今日已用次数
      "image": 1,
      "video": 0
    },
    "daily_limit": {
      "chat": 20,
      "image": 5,
      "video": 1
    }
  },
  // ---- 硬件插件注册表 ----
  "plugins": [
    {
      "id": "my-robot-car",
      "name": "智能小车",
      "version": "1.0.0",
      "enabled": true,
      "installed_at": "2026-06-24T10:00:00Z",
      // 插件的积木代码嵌入存储（或引用 CDN URL）
      "source_type": "inline",       // inline | url
      "manifest": { /* manifest.json 副本 */ },
      "blocks_code": "...",          // blocks.js 源码
      "runtime_code": "..."          // runtime.js 源码
    }
  ],
  // ---- 硬件连接配置 ----
  "hardware": {
    "last_device": "micro:bit",
    "connection_type": "serial",     // serial | bluetooth | websocket
    "baud_rate": 115200,
    "auto_connect": false
  }
}
```

### 5.2 用户数据结构

```jsonc
{
  "version": "1.0",
  "user": {
    "id": "user_001",
    "username": "xiaoming",
    "display_name": "小明",
    "role": "student",
    "grade": 3,
    "parent_email": "parent@example.com",
    "created_at": "2026-06-01T00:00:00Z",
    "settings": {
      "language": "zh-CN",
      "theme": "light",
      "auto_preview": true,
      "agnes_api_key": "",               // Agnes API Key（由用户或家长输入）
      "credits_warning_at": 50,          // 余额低于此值时弹窗提醒充值
      "daily_limit": {
        "chat": 20,
        "image": 5,
        "video": 1
      },
      "hardware": {
        "auto_connect": false,
        "allowed_permissions": ["serial", "bluetooth"],
        "plugin_auto_load": true
      }
    }
  },
  "stats": {
    "project_count": 12,
    "total_blocks": 345,
    "ai_queries_today": 3,
    "ai_queries_total": 87
  }
}
```

---

## 第六章：技术架构

### 6.1 架构总览

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         Tauri Desktop Shell (Rust)                        │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                   WebView (系统原生渲染引擎)                         │  │
│  │                                                                      │  │
│  │  ┌─────────────────────┬─────────────────────────┬───────────────┐  │  │
│  │  │                     │                         │               │  │  │
│  │  │    积木区            │   脚本编辑区             │  舞台区       │  │  │
│  │  │  (标准积木 + 硬件    │   (积木拖拽/组合)       │  (TurboWarp   │  │  │
│  │  │   插件扩展积木)      │                         │   编译器运行) │  │  │
│  │  │                     │                         │               │  │  │
│  │  └─────────────────────┴─────────────────────────┴───────────────┘  │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │  底部面板 (Bottom Panel) — 标签切换，高度可调                  │  │  │
│  │  │  ◉ AI 对话  │  ○ 素材工坊  │  ○ 硬件  │  ○ 插件              │  │  │
│  │  │  ┌──────────────────────────────────────────────────────┐   │  │  │
│  │  │  │  当前标签页内容 (独立状态，切换不丢失)                 │   │  │  │
│  │  │  └──────────────────────────────────────────────────────┘   │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  │                                                                      │  │
│  │  ┌────────────────────────────────────────────────────────────┐    │  │
│  │  │  Agnes AI 引擎   (使用说明见 src/lib/agnes/)               │    │  │
│  │  │  ┌────────────┐  ┌────────────┐  ┌────────────┐          │    │  │
│  │  │  │ Chat       │  │ Image      │  │ Video      │          │    │  │
│  │  │  │ 积木生成   │  │ 角色/背景  │  │ 造型动画   │          │    │  │
│  │  │  │ 对话调试   │  │ 生成       │  │ 生成+截帧  │          │    │  │
│  │  │  └─────┬──────┘  └──────┬─────┘  └──────┬─────┘          │    │  │
│  │  │        └───────────────┼─────────────────┘                │    │  │
│  │  │                   Credits 管理 (检查/消耗/充值引导)       │    │  │
│  │  └────────────────────────────────────────────────────────────┘    │  │
│  │                                                                      │  │
│  │  ┌──────────────────────────────┐  ┌──────────────────────────────┐  │  │
│  │  │  插件运行时                   │  │  素材工坊引擎                │  │  │
│  │  │  ┌──────────┐ ┌───────────┐  │  │  ┌──────┐ ┌──────┐ ┌───┐  │  │  │
│  │  │  │内置硬件   │ │用户插件  │  │  │  │去背景│ │AI    │ │裁  │  │  │  │
│  │  │  │积木引擎   │ │Web Worker│  │  │  │(ONNX)│ │重绘  │ │剪  │  │  │  │
│  │  │  │(Serial/   │ │沙箱      │  │  │  └──────┘ └──────┘ └───┘  │  │  │
│  │  │  │ Bluetooth) │ │         │  │  └──────────────────────────────┘  │  │
│  │  │  └──────────┘ └───────────┘  │                                   │  │
│  │  └──────────────────────────────┘                                   │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  Tauri Rust 后端 (IPC 桥接)                                         │  │
│  │  ┌────────────┐  ┌───────────┐  ┌──────────┐  ┌───────────────┐  │  │
│  │  │ serialport │  │ btleplug  │  │ 文件系统  │  │ 系统集成      │  │  │
│  │  │ (串口)     │  │ (蓝牙)    │  │ 插件/项目 │  │ 窗口/托盘/通知│  │  │
│  │  └────────────┘  └───────────┘  └──────────┘  └───────────────┘  │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌──────────────────────────────┐  ┌──────────────────────────────────┐  │
│  │  Agnes AI (云端)             │  │  本地文件系统                     │  │
│  │  https://apihub.agnes-ai.com │  │  ~/.scratchmind/                 │  │
│  │  • Chat API                  │  │  ├── plugins/user/  (用户插件)   │  │
│  │  • Image API                 │  │  ├── plugins/built-in/ (内置)    │  │
│  │  • Video API                 │  │  ├── projects/ (项目文件)        │  │
│  │  • Credits 余额查询          │  │  └── config.json (设置)          │  │
│  └──────────────────────────────┘  └──────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
```

### 6.2 技术选型及理由

| 层 | 选型 | 理由 | 可替换性 |
|------|------|------|---------|
| 前端框架 | Next.js 14+ | SSR 利于 SEO，API Routes 可快速搭建 BFF 层 | 可替换为 Nuxt |
| 积木引擎 | TurboWarp（scratch-vm 编译器优化版） | 完全兼容 .sb3，性能提升 10-100x，支持 60FPS，自定义扩展无需改核心 | 高成本替换 |
| AI 交互 | SSE（Server-Sent Events） | 流式输出打字机效果，对儿童有"AI正在思考"的视觉反馈 | 可替换为 WS |
| LLM 主选 | OpenCode 免费 API | 零成本，适合 MVP 和个人课堂使用 | **产品核心可切换设计** |
| LLM 备选 | DeepSeek API | 国产可替代，成本低，中文理解好 | 见上 |
| LLM 自建 | 自定义 API（兼容 OpenAI 协议） | 学校/机构私有化部署 | 见上 |
| 数据库 | PostgreSQL | 存结构化数据和 JSONB 项目数据 | SQLite（开发阶段） |
| 静态资源 | MinIO / S3 | 存储用户上传的角色素材 | 本地文件系统（开发） |
| 安全过滤 | 关键词规则 + LLM as judge | 双层过滤，对儿童产品是强需求 | 可接入腾讯/阿里云审核 |
| 儿童认证 | 无（家长代为注册） | 合规要求，14 岁以下需家长同意 | 不可替换 |
| 硬件通信 | Tauri + serialport crate + btleplug | 桌面原生串口/蓝牙，不依赖浏览器 | 不可替换（架构决策） |
| 素材编辑 | Canvas 2D API + ONNX Runtime（本地去背景） | 离线可用，无需网络 | 可替换为 WASM 方案 |
| 插件沙箱 | Web Worker | 天然沙箱隔离，无 DOM 访问权限 | 可替换为 iframe 沙箱 |
| 插件格式 | 自定 manifest + blocks.js + runtime.js | 轻量，JSON + 纯函数注册 | 可参考 ScratchX 或 Figma 插件规范 |

### 6.3 LLM 适配层设计（核心架构决策）

```
┌─────────────────────────────────────────────┐
│                LLM 适配层                     │
│                                              │
│  Provider 路由                               │
│  ┌───────────────────────────────────────┐   │
│  │  ProviderInterface (抽象接口)          │   │
│  │  ┌──────────┐ ┌──────────┐ ┌───────┐ │   │
│  │  │OpenCode  │ │DeepSeek  │ │Custom │ │   │
│  │  │Provider  │ │Provider  │ │Provider│ │   │
│  │  └──────────┘ └──────────┘ └───────┘ │   │
│  └───────────────────────────────────────┘   │
│                                              │
│  统一输出格式：scratch-vm 积木定义 JSON       │
│  + 中文解释文案（儿童友好）                    │
│                                              │
│  ProviderInterface 定义：                     │
│  - chat(messages, projectContext) → Result   │
│  - name: string                              │
│  - isAvailable(): boolean                    │
│  - rateLimit: { maxPerMin, maxPerDay }       │
│  - costPerCall: number (用于统计)             │
└─────────────────────────────────────────────┘
```

**硬约束**：
- Provider 路由必须支持**运行时切换**——不重启服务，不做部署变更
- OpenCode 免费 API 为默认 Provider，若无配置直接可用
- DeepSeek 和自定义 API 由用户在设置页面填写 API Key 后启用
- 所有 Provider 的输出必须经过统一的安全过滤层和积木验证器
- 当当前 Provider 失败时，若有可用的备选 Provider，自动切换重试（儿童无感知）

**发挥空间**：
- 增加 Provider 健康检测（心跳检查）
- 增加多 Provider 并行调用、取最佳结果的能力
- 增加使用统计仪表盘（当日各 Provider 调用次数、成功率）

### 6.4 Prompt 架构

```
System Prompt 概览:
  你是一个 Scratch 积木编程老师，正在教 6-14 岁的小朋友。
  小朋友用中文告诉你他们想让角色做什么，
  你需要生成对应的 Scratch 3.0 积木。

  关键约束：
  1. 只输出 JSON 格式的积木定义（scratch-vm 兼容）
  2. 每条积木必须语法完整，不能产生无效积木
  3. 优先使用最直观、积木数量最少的方案
  4. 用 6 岁孩子能听懂的中文解释每块积木的作用
     - 不要用：循环、变量、参数、布尔值
     - 要用：重复做、小盒子、设置、是对的还是错的
  5. 如果需求无法用标准 Scratch 积木实现，说明限制并提供替代方案
  6. 生成内容必须安全、积极、适合儿童

  输出格式：
  {
    "explanation": "用大哥哥/大姐姐的语气解释积木的作用",
    "blocks": { ... scratch-vm 格式的积木树 ... },
    "suggestions": ["还可以试试：...", "想让它更快吗？试试..."]
  }
```

### 6.5 插件系统设计

```
┌──────────────────────────────────────────────────────────┐
│                   插件系统架构                             │
│                                                          │
│  积木编辑器                                                │
│    │                                                      │
│    ▼                                                      │
│  ┌──────────────────────────────────────────────────┐     │
│  │           Plugin Manager (主线程)                 │     │
│  │  • 管理插件注册表                                  │     │
│  │  • 将插件积木注入 scratch-blocks 积木分类           │     │
│  │  • 将积木 opcode 映射到 Worker 消息通道            │     │
│  └──────────┬───────────────────────────────────────┘     │
│             │                                              │
│    ┌────────┴────────┐                                    │
│    │  通信桥 (MsgBus) │                                    │
│    └────────┬────────┘                                    │
│             │ postMessage                                  │
│    ┌────────┴──────────────────────────────────────┐     │
│    │         Web Worker 沙箱                         │     │
│    │  ┌────────────────────────────────────────┐   │     │
│    │  │  Plugin A (智能小车)                     │   │     │
│    │  │  • block1 → handler1                   │   │     │
│    │  │  • block2 → handler2                   │   │     │
│    │  └────────────────────────────────────────┘   │     │
│    │  ┌────────────────────────────────────────┐   │     │
│    │  │  Plugin B (温湿度传感器)                 │   │     │
│    │  │  • block1 → handler1                   │   │     │
│    │  └────────────────────────────────────────┘   │     │
│    └────────────────────────────────────────────────┘     │
│             │                                              │
│             ▼ postMessage                                  │
│  ┌──────────────────────────────────────────────────┐     │
│  │          Hardware Bridge (主线程)                  │     │
│  │  • 原生 Serial Port (serialport crate)            │     │
│  │  • Web Bluetooth API                              │     │
│  │  • WebSocket                                      │     │
│  │  • 通用硬件协议解析 (JSON 指令 ↔ 硬件)             │     │
│  └──────────────────────────────────────────────────┘     │
│             │                                              │
│             ▼ 串口/蓝牙/网络                               │
│        [ 硬件设备 ]                                       │
└──────────────────────────────────────────────────────────┘
```

**插件系统关键设计决策**：

| 决策 | 选择 | 理由 |
|------|------|------|
| 沙箱方案 | Web Worker | 天然无 DOM 访问权，性能好，postMessage 通信清晰 |
| 积木注册方式 | 声明式（manifest 定义积木签名）+ 命令式（blocks.js 提供实现） | 声明式用于 UI 渲染，命令式用于运行时执行 |
| 通信协议 | JSON 双向消息通道 | 简单可调试，硬件端也易于解析 |
| 插件打包格式 | 单个 .zip（含 manifest.json + blocks.js + runtime.js） | 上传方便，解压后逐个验签 |
| **插件持久存储** | **IndexedDB（浏览器主存）+ `plugins/` 目录（服务端源码）** | 关页不丢，重启自动恢复；服务端目录用于内置插件和可选同步 |
| **启动加载方式** | **Plugin Manager 初始化时扫描 IndexedDB 全部已安装插件** | 自动恢复上次会话的所有插件，无需用户干预 |
| **内置插件来源** | **项目 `plugins/built-in/` 目录，构建时编译注入** | 随代码发布，版本管理清晰 |
| 插件市场 | 可选，P2 范围 | 插件目录本身是 JSON 列表，可静态托管 |

**硬约束**：
- 每个 Worker 同时只能运行一个插件的积木调用（一个项目可能有多插件，但积木执行必须串行）
- 内置硬件积木使用与用户插件相同的 Worker 沙箱——不享有特权
- 插件不可访问 `localStorage`、`IndexedDB`、`fetch`、`XMLHttpRequest`
- **Plugin Manager 必须在编辑器加载完成前初始化完毕**——确保积木区展现完整后才可交互
- 插件安装/卸载不可影响已打开的项目——项目只引用 `plugin_id`，不嵌入插件代码

**发挥空间**：
- 插件可声明 `"ai_context": true` 表示其积木参与 AI 上下文，AI 可理解并生成该插件的积木
- 插件市场 + 评分系统
- 可视化插件生成器——拖拽定义积木签名，AI 辅助生成 blocks.js 骨架
- 服务端插件同步——登录用户在不同电脑上拥有相同的已安装插件列表

---

## 第七章：交互细节

### 7.1 AI 对话交互流程（儿童版）

AI 对话面板位于底部面板的"AI 对话"标签页。孩子点击标签展开，输入完可收起，不影响主编辑区。

```
孩子点击底部 "AI 对话" 标签
    │
    ▼
底部面板展开，显示对话框
    │
    ▼
孩子输入文字
    │
    ▼
[输入校验] ──空或太短──→ 显示示例建议"试试说：让小猫走起来"
    │
    ▼ (通过)
[安全过滤] ──触发──→ "这个话题不太合适哦，换个点子吧"
    │
    ▼ (通过)
[检查 Credits 余额]
    │
    ├── 余额不足 → 底部浮层"今天免费次数用完啦" + [让爸妈充值]
    │
    ▼ (余额充足)
[AgnesRouter 判断请求类型]
    │
    ├── 积木需求 → agnes.chat() → SSE 流式返回
    │   ├── 成功 → [积木验证] → 通过 → 展示积木列表 + [插入到编辑器]
    │   │                              └── 不通过 → 自动重试(x1) → 失败则"换个说法试试"
    │   └── 失败 → "网络开小差了" + 重试按钮
    │
    ├── 角色/背景需求 → agnes.generateImage()
    │   ├── 成功 → 展示图片预览 + [导入为角色] [导入为背景] [去素材工坊编辑]
    │   └── 失败 → "画不出来，换个描述？"
    │
    └── 动画需求 → agnes.generateVideo() → 轮询进度(30-60s)
        ├── 成功 → 截帧 → 导入造型 → "动画做好了！在角色的造型里可以看到"
        └── 失败/超时 → "拍视频超时了，换个短一点的试试"
```

### 7.2 AI 回复渲染规则

**推荐默认**：
1. AI 回复分两部分：文字说明 + 积木列表
2. 文字说明在前，用对话气泡展示，语气亲切如老师
3. 积木列表在文字下方，每行一个带 Scratch 积木图标的说明
4. "插入到编辑器"按钮在积木列表底部
5. 插入后编辑器自动滚动到新积木位置，高亮 2 秒

**硬约束**：
- 插入积木必须可撤销（Ctrl+Z 移除本次生成的所有积木）
- 孩子可以点击"换一个"让 AI 重新生成
- 所有 AI 回复文案需由安全过滤器二次检查

### 7.3 AI 设置与 Credits 充值

```
用户/家长进入 设置 → AI 设置

┌────────────────────────────────────┐
│  AI 老师设置（家长控制）           │
│                                    │
│  Agnes API Key                     │
│  [••••••••••••••••••••••••]       │
│  [验证 Key] → ✅ 有效，余额 200   │
│                                    │
│  免费额度（每日）                  │
│  积木生成：20 次  今日已用 3 次    │
│  图片生成：5 次   今日已用 1 次    │
│  视频生成：1 次   今日已用 0 次    │
│                                    │
│  Credits 余额：200 🪙              │
│  [充值 Credits] → 打开 Agnes 充值  │
│  页面                               │
│                                    │
│  余额低于 50 时提醒 ✓              │
│                                    │
│  [恢复默认额度]                    │
└────────────────────────────────────┘
```

**硬约束**：
- API Key 输入需要家长密码保护（简单密码，区别于儿童模式）
- 充值跳转 Agnes 官方支付页面，平台不代收
- 免费额度每日零点重置（基于本地时钟）
- Credits 余额从 Agnes API 实时查询，每 5 分钟自动刷新

### 7.4 硬件连接交互

```
┌────────────────────────────────┐
│  硬件连接                      │
│                                │
│  [🔌 连接设备] ───→ 弹出浏览   │
│       │              器串口选择 │
│       ▼                       │
│  [连接中...]                   │
│       │                       │
│  ┌────┴────┐                  │
│  │  ✅ 已连接 │  ❌ 连接失败    │
│  │ micro:bit│  "请检查USB线"  │
│  │ COM3     │  [重试]         │
│  │ [断开]   │                 │
│  └─────────┘                  │
│                                │
│  硬件状态面板（运行中实时显示）│
│  ┌────────────────────────┐   │
│  │ 🔧 micro:bit (🟢已连接) │   │
│  │ 温度: 26.5°C            │   │
│  │ LED: ● ● ○ ● ○         │   │
│  │ 按键A: 未按下            │   │
│  │ 最近通信: 2秒前          │   │
│  └────────────────────────┘   │
└────────────────────────────────┘
```

**正常流程**：
1. 孩子点击"🔌 连接设备"按钮
2. 浏览器弹出串口/蓝牙设备选择列表
3. 选择对应硬件设备
4. 连接成功后，硬件状态面板实时显示设备信息和传感器数据
5. 积木区"硬件"分类的积木变为可用
6. 孩子拖拽硬件积木，运行时自动通过串口/蓝牙发送指令

**失败路径 1 — 串口被占用**：
- 场景：另一个软件正在使用该串口（如 Arduino IDE 的串口监视器）
- 行为：弹窗提示"设备被其他程序占用，请关闭后再试"
- 降级：列出当前系统可用串口列表，推荐未占用的端口
- 状态：保持在 [未连接]

**失败路径 2 — 设备被占用**：
- 场景：另一个软件或标签页正在使用串口
- 行为：弹窗提示"设备被其他程序占用，请关闭后再试"
- 状态：保持在 [未连接]

**失败路径 3 — 运行中设备断开**：
- 场景：拔掉 USB 线或蓝牙断连
- 行为：舞台自动停止，硬件状态面板变红，提示"设备断开啦"
- 恢复：自动尝试重连（3 秒间隔，最多 3 次），失败则提示手动操作

**硬约束**：
- 所有硬件连接必须在用户手势触发（click 事件）下进行，不得自动连接
- 连接失败时必须给出具体原因（"设备被占用""找不到设备""驱动异常"），不得只显示"连接失败"
- 硬件积木在未连接状态下不可拖拽（灰色不可用），或拖拽后提示"请先连接设备"

### 7.5 素材工坊交互

素材工坊位于底部面板第二个标签页。可从两个入口进入：
- **AI 对话中生成图片后** → 点击"[去素材工坊编辑]" → 自动切换到素材工坊标签，加载当前素材
- **双击角色列表中的造型** → 直接打开素材工坊，加载该造型

```
双击角色造型 / 点击"去素材工坊"
    │
    ▼
底部面板切换到"素材工坊"标签
    │
    ▼
素材加载到预览区域
    │
    ▼
用户选择编辑工具
    │
    ├── 去背景 → 本地 ONNX 模型处理(1-3秒) → 实时预览透明效果
    │              │
    │              ├── 效果满意 → [保存]
    │              └── 效果不佳 → 画笔手动修正 → [保存]
    │
    ├── AI 重绘 → 弹出描述输入框"想改成什么样？"
    │              │
    │              ▼
    │          调用 Agnes Image API inpainting → 预览 → [保存] [放弃]
    │
    └── 裁剪/旋转/翻转 → Canvas 实时操作 → [保存]
    │
    ▼
[保存] → 覆盖原造型 或 另存为新造型
    │
    ▼
角色列表即时更新，新造型可拖积木使用
```

**失败路径 1 — 素材未选择**：
- 场景：用户打开素材工坊但未选择任何素材
- 行为：工具按钮灰色不可用，提示"先点选一个角色造型再编辑"
- 状态：保持在素材工坊，工具不可用

**失败路径 2 — ONNX 模型加载失败**：
- 场景：去背景模型初次加载时模型文件损坏或缺失
- 行为：降级调用 Agnes Image API 做去背景（需联网）
- 状态：提示"本地去背景不可用，改用 AI 在线去背景"，功能不受影响

**失败路径 3 — 编辑后未保存切换标签**：
- 场景：编辑了素材但没点保存，切换到"AI 对话"标签
- 行为：底部面板显示"素材工坊"标签上有红点标记 ⏺️（未保存）
- 恢复：切回素材工坊，编辑状态完整保留
- 只有关闭素材工坊（切换项目或关闭应用）时弹窗确认保存

**硬约束**：
- 编辑状态跨标签切换不丢失（AI 对话 ↔ 素材工坊 ↔ 硬件 来回切，编辑内容都在）
- 每次保存自动创建版本备份（最多保留 5 个历史版本），用户可回退

---

## 第八章：导出与输出系统

### 8.1 支持格式

| 格式 | 用途 | P0/P1 |
|------|------|-------|
| `.sb3` | Scratch 3.0 标准格式，可导入原生 Scratch 或 TurboWarp | P0 |
| 分享链接 | 智搭平台上预览和 Remix | P0 |
| HTML 快照 | 导出为独立 HTML 文件，离线可运行 | P1 |
| `.zhplugin` | 插件打包格式（zip 重命名），可分享给其他用户安装 | P1 |
| GIF 录屏 | 录制项目运行的 5 秒 GIF（孩子最爱分享这个） | P2 |

### 8.2 导出流程

```
孩子点击 文件 → 导出
    │
    ├── 保存到电脑 → 浏览器下载 .sb3 文件
    │
    ├── 分享给朋友 → 生成 6 位短码 → 复制链接
    │
    └── 导出为网页 → 独立 HTML 文件（可离线打开）
```

**硬约束**：
- `.sb3` 导出必须与 Scratch 3.0 完全兼容
- 分享链接支持匿名访问（无需登录即可预览）

---

## 第九章：开发优先级

### P0 — MVP 核心

| # | 功能 | 说明 |
|---|------|------|
| 1 | 积木编辑器基座（TurboWarp 集成） | 积木区 + 脚本编辑区 + 角色管理 |
| 2 | 舞台与 VM 运行 | TurboWarp 编译器模式，60FPS |
| 3 | **底部面板框架** | **标签切换 + 可折叠/调高 + 跨标签状态保持** |
| 4 | AI 对话面板（底部面板标签1） | 输入框、消息列表、积木/图片/视频结果展示 |
| 5 | Agnes AI 引擎（Chat） | NL→积木生成 + Credits 检查 + 安全过滤 |
| 6 | 项目保存/加载（本地文件系统） | 自动保存 `~/.scratchmind/projects/` |
| 7 | `.sb3` 导入导出 | 兼容 Scratch 3.0 / TurboWarp 格式 |
| 8 | 基础硬件积木 | 内置 LED/按键/舵机/蜂鸣器积木，原生串口通信 |

### P1 — 体验增强

| # | 功能 | 说明 |
|---|------|------|
| 9 | Agnes AI 引擎（Image + Video） | 角色/背景生成 + 造型动画生成 |
| 10 | **素材工坊基础工具** | **去背景(ONNX) + 裁剪 + 旋转 + 翻转 + 保存造型** |
| 11 | AI 调试功能 | 诊断已有积木问题 |
| 12 | 自动预览模式 | AI 生成积木后自动运行 |
| 13 | 用户插件系统 | manifest + blocks.js + runtime.js，Web Worker 沙箱 |
| 14 | **素材工坊进阶工具** | **AI 重绘 + 智能切割 + 画笔微调** |
| 15 | 硬件面板（底部面板标签3） | 串口连接 + 状态监视 |
| 16 | 插件管理面板（底部面板标签4） | 安装/卸载/管理 |
| 17 | Credits 充值引导 | 余额不足提醒 + 充值入口 |

### P2 — 差异化

| # | 功能 |
|---|------|
| 18 | AI 一键生成完整项目 |
| 19 | AI 积木解说员（点击积木解释） |
| 20 | 语音输入 |
| 21 | AI 写硬件插件（NL→插件代码） |
| 22 | 批量素材编辑（多造型统一去背景） |
| 23 | 帧动画预览（多造型连播） |
| 24 | 滤镜效果（像素化/油画/黑白） |
| 25 | AI 扩图 |
| 26 | Web Bluetooth 支持 |
| 27 | 教师管理后台 |
| 28 | 官方硬件插件库（micro:bit/Arduino/ESP32参考固件） |
| 29 | 插件市场（社区上传/下载插件） |

### MVP 定义

MVP = P0 全部完成。此时孩子可以正常使用：积木编辑器工作（TurboWarp 引擎 + 60FPS）、AI 对话生成积木、项目可保存加载、可连接硬件设备用基础积木控制。

**推荐默认**：MVP 无需用户系统，所有项目保存在浏览器本地存储。体验完整后提示注册以开启云存储和分享。硬件功能在 MVP 即可用（串口连接 + 内置硬件积木），用户插件系统在 P1 开放。

---

## 第十章：性能指标

### 10.1 关键指标

| 指标 | 目标值 | 测量方法 | 降级阈值 | 降级行为 |
|------|--------|---------|---------|---------|
| 页面首次加载 | ≤ 3 秒 | Lighthouse FCP | > 5 秒 | 显示骨架屏，lazy load |
| 积木拖拽帧率 | ≥ 55fps | DevTools Performance | < 30fps | 提示关闭动画效果 |
| 舞台运行帧率 | 60FPS（编译器模式） | TurboWarp FPS 计数器 | < 30FPS | 自动降级为 30FPS 解释器模式 |
| LLM 首字节 | ≤ 2 秒（SSE） | 从发送到首个 chunk | > 5 秒 | 显示"正在想..."，10 秒超时 |
| LLM 全响应 | ≤ 8 秒 | 从发送到完整响应 | > 15 秒 | 超时后切换 Provider 或提示重试 |
| 安全过滤延迟 | ≤ 500ms | 过滤模块日志 | > 1 秒 | 允许通过但标记人工复核 |
| Provider 切换 | ≤ 1 秒 | 从检测失败到切换到备选 | > 3 秒 | 直接提示用户检查网络 |
| `.sb3` 导入 | ≤ 3 秒（10MB） | 手动计时 | > 10 秒 | 提示文件过大 |
| 硬件连接 | ≤ 3 秒 | 从点击连接到串口/蓝牙就绪 | > 5 秒 | 提示"连接超时，检查设备" |
| 硬件指令延迟 | ≤ 50ms | 积木执行到串口发出数据 | > 200ms | 提示"通信延迟较高" |
| 插件加载 | ≤ 2 秒 | 从选择文件到积木出现在编辑区 | > 5 秒 | 提示"插件体积过大" |
| 插件沙箱内存 | ≤ 50MB per plugin | Worker heap 监控 | > 100MB | 终止 Worker，提示"插件占用内存太多" |

### 10.2 性能预算

**推荐默认**：首屏 JS 体积 ≤ 500KB（gzipped）。TurboWarp 的 UMD 构建约 800KB gzipped，需从本地文件系统加载（跳过网络），不影响加载速度。

**硬约束**：
- AI 面板必须异步加载，不阻塞积木编辑器渲染
- LLM 调用不可阻塞 UI 线程

---

## 第十一章：开发者交接说明

### a) 先做什么

第一步初始化 Tauri + TurboWarp 集成，第二步实现 Agnes AI 引擎（Chat+Image+Video），第三步做 AI 对话面板，第四步做硬件扩展+插件系统。

**TurboWarp 集成要点**：
- TurboWarp 是开源项目（[GitHub](https://github.com/TurboWarp)），提供 UMD 构建可直接在 WebView 中加载
- 通过 `vm.loadProject()` 加载 `.sb3` 项目，`vm.greenFlag()` / `vm.stopAll()` 控制运行
- TurboWarp 内置编译器：`vm.runtime.setCompilerOptions({ enabled: true })` 开启 JIT
- 自定义扩展通过 `vm.extensionManager.register()` 注册——插件系统利用此 API 注入积木
- TurboWarp 的扩展 API 自带打包工具，可将插件编译为 `.js` 扩展文件

**Agnes 引擎核心接口**：

```typescript
// src/lib/agnes/agnes-engine.ts

interface AgnesEngine {
  // Chat：自然语言 → 积木定义
  chat(messages: ChatMessage[], projectContext: ProjectContext): Promise<ChatResult>;

  // Image：描述 → 角色/背景图片 URL
  generateImage(prompt: string, options?: {
    background?: "white" | "transparent";
    style?: "cartoon";
  }): Promise<ImageResult>;

  // Video：描述 → 造型变化动画
  generateVideo(prompt: string, options?: {
    duration: number;  // 1-3 秒
  }): Promise<VideoResult>;

  // Credits 管理
  getCreditsBalance(): Promise<number>;
  getDailyUsage(): Promise<DailyUsage>;
}

interface ChatResult {
  success: boolean;
  explanation: string;           // 儿童友好文字
  blocks: Record<string, BlockDefinition>;
  suggestions: string[];
  credits_consumed: number;
  latency_ms: number;
}

interface ImageResult {
  success: boolean;
  image_url: string;
  credits_consumed: number;
}

interface VideoResult {
  success: boolean;
  video_url: string;
  frames: string[];             // 截帧后的造型图 URL
  credits_consumed: number;
}
```

**Agnes 路由逻辑（简化）**：

```
用户输入 → AI Panel
    │
    ▼
AgnesRouter 判断请求类型：
  ├─ 积木需求 → agnes.chat() → 积木验证 → 展示 + [插入编辑器]
  ├─ 角色/背景需求 → agnes.generateImage() → 预览 → [导入为角色/背景]
  └─ 动画需求 → agnes.generateVideo() → 轮询(30-60s) → 截帧 → 导入造型
    │
    ▼
每次调用前检查 credits 余额
余额不足 → 弹浮层"让爸爸妈妈充值吧" + [充值入口]
```

### b) 不要重新发明

| 不要做 | 应该用 |
|--------|--------|
| 自研积木引擎 | TurboWarp（scratch-vm 编译器优化版，GPL v2） |
| 自研舞台渲染 | TurboWarp 内置（WebGL 加速 + JIT 编译） |
| 自研音频引擎 | TurboWarp 内置 |
| 自研 LLM SDK | 各 Provider 的官方 SDK 或标准 fetch |
| 自研桌面框架 | Tauri（Rust 后端 + WebView 渲染） |
| 自研串口库 | serialport crate（Rust） |
| 自研蓝牙库 | btleplug crate（Rust） |
| 自研沙箱环境 | Web Worker（浏览器原生沙箱） |
| 自研插件打包格式 | .zip 简单打包（manifest + 源码），参考 VSCode 扩展规范 |

### c) 可以自由发挥的部分

- AI 对话面板的 UI 设计（儿童友好的动画、色彩、表情）
- 安全过滤的自定义规则库
- 作品集市的推荐算法
- Provider 健康检测的实现细节
- 硬件状态面板的 UI（实时数据显示样式）
- 参考固件的代码质量（Arduino/micro:bit/ESP32）
- 插件市场的推荐算法和审核流程

### d) 已知的未知项

1. **Agnes 免费 API 的速率限制与稳定性**：需上线后实测。建议 MVP 在客户端做调用次数统计，免费额度耗尽后引导充值
2. **Agnes Image API 生成 Scratch 素材的适用性**：角色图片需透明背景或白底，卡通风格是否一致——需对比测试不同 prompt 策略
3. **Agnes Video API 视频截帧质量**：生成的动画截取首尾帧作为造型切换，需要验证截帧准确率和角色一致性
4. **Scratch 素材版权**：Scratch 素材库是 CC BY-SA 2.0，应用内需加署名
5. **儿童隐私合规**：桌面端不收集个人信息，但 AI 调用会发送文本/图片到 Agnes API。需在首次启动时向家长说明
6. **Tauri 安全模型**：Web Worker 通过 Tauri `invoke` 调用 Rust 后端命令时，需确保权限控制严格——Worker 只能调用已注册的硬件命令，不能任意执行系统命令
7. **跨平台串口兼容性**：Linux 需 `dialout` 用户组，macOS 需授权，Windows 需驱动。首次启动需检测并引导
8. **硬件协议标准化**：不同硬件（micro:bit vs Arduino vs ESP32）的 JSON 指令实现可能不一致，MVP 优先支持 micro:bit 和 Arduino

### e) 验收剧本

实施完 P0 后，请运行以下验收：

```
# 验收 1：积木编辑器
1. 打开 /create → 看到积木区 + 脚本编辑区 + 舞台
2. 拖拽"移动10步"到脚本区 → 积木吸附
3. 拖拽"重复执行"包围"移动10步" → 嵌套成功
4. 点击绿旗 → 角色移动
5. Ctrl+Z 撤销 → 积木消失

# 验收 2：AI 对话（默认 OpenCode 免费 API）
1. 在 AI 面板输入"让小猫走10步"
2. 看到小机器人思考动效
3. 3-8 秒内 AI 返回积木列表 + 中文说明
4. 点击"插入到编辑器" → 积木出现在脚本区
5. 点击绿旗 → 验证积木正确执行
6. 再次输入"每次开始都随机位置" → AI 基于已有积木补充

# 验收 3：LLM Provider 切换
1. 进入设置 → AI 设置 → 切换到 DeepSeek
2. 输入 API Key → 点击"测试连接" → 显示"连接成功"
3. 回到编辑器，发送"让小猫走路" → 使用 DeepSeek 后端
4. 再次切换到 OpenCode 免费 API → 正常使用
5. 故意填错 API Key → 显示错误提示，自动切回 OpenCode

# 验收 4：失败路径
1. 断网后发送 AI 请求 → "网络开小差了"提示
2. 发送暴力内容 → 安全过滤器拦截
3. 输入空字符串 → 显示示例建议

# 验收 5：安全（儿童保护）
1. 发送"做炸弹" → 安全过滤器拦截
2. LLM 返回包含不适内容的响应 → 安全过滤器拦截，不展示
3. 验证安全过滤日志有记录（供家长审查）

# 验收 6：保存/加载
1. 点击保存 → 项目保存到 localStorage
2. 刷新页面 → 项目自动恢复
3. 导出 .sb3 → 下载文件
4. 将 .sb3 导入原生 Scratch → 积木和角色完全正常

# 验收 7：硬件连接 + 基础积木
1. 点击"连接设备" → 弹出系统串口选择列表（显示可用 COM 端口）
2. 选择 micro:bit/Arduino（烧录了智搭固件）→ 状态显示"🟢已连接"
3. 拖拽"设置 LED 引脚 13 为 高电平" → 硬件板载 LED 亮
4. 拖拽"设置 LED 引脚 13 为 低电平" → LED 灭
5. 拖拽"蜂鸣器 引脚 8 响 1 秒" → 蜂鸣器响
6. 断开 USB → 状态变红 "❌ 连接断开"

# 验收 8：用户插件系统（P1 范围）
1. 准备一个插件 .zhplugin（含 manifest.json + blocks.js + runtime.js）
2. 点击"安装插件" → 选择 .zhplugin → 积木区新增 "硬件 > 智能小车" 分类
3. 拖拽插件积木到脚本区 → 积木显示正常，参数可调
4. 运行 → 插件 Worker 启动，硬件正常响应
5. **关闭应用、重新打开** → 插件自动加载，积木区仍然显示 "硬件 > 智能小车"
6. 卸载插件 → 积木区恢复
7. **再重启** → 插件已移除，积木区不再显示该分类

# 验收 9：性能
1. DevTools Performance → 拖拽积木帧率 ≥ 55fps
2. Network → AI 请求首字节 ≤ 2s
3. Lighthouse → FCP ≤ 3s（含 CDN）
4. 硬件指令延迟 ≤ 50ms（从积木执行到串口数据发出）
```

### f) 给 AI 助手的话

你负责实现本 PRD 描述的模块。**安全过滤层不能跳过——那是儿童产品的生命线。** 所有 AI 生成内容需经验证才能展示。

**Agnes AI 引擎**是核心。务必先实现 `agnes-engine.ts` 接口（chat + generateImage + generateVideo），再实现 UI。这三个 API 调用都通过同一个 `AGNES_API_KEY` 认证，base URL 为 `https://apihub.agnes-ai.com/v1`。

**Tauri 集成注意**：
- TurboWarp 直接在 WebView 中运行，用 Tauri 的 WebView (webkit2gtk on Linux, WebView2 on Windows, WKWebView on macOS)
- 硬件串口在 Rust 后端用 `serialport` crate——通过 Tauri `#[tauri::command]` 暴露给前端
- 插件目录 `~/.scratchmind/plugins/` 在 Rust 端创建和扫描，前端通过 invoke 获取列表
- 窗口标题建议显示 "智搭 - [项目名]" + 硬件连接状态图标
- 关闭窗口时自动保存当前项目到 `~/.scratchmind/projects/` 下的恢复文件

**Credits 充值引导实现要点**：
- `credits.ts` 模块负责追踪余额，每次 AI 调用前检查
- 余额不足时非阻塞提示（底部浮层），不阻断编辑器使用
- 充值链接跳转到 Agnes 官方充值页面，填写用户 API Key 关联
- 免费额度每日零点刷新（基于本地时钟，不强依赖服务端）

**积木验证器是第二道防线**——即使 AI 返回了格式完美的 JSON，也要验证每个积木的 opcode 是否存在、inputs 结构是否正确。

**插件系统**请优先实现内置硬件积木（P0），再实现用户插件加载（P1）。

**插件持久化**——`~/.scratchmind/plugins/user/` 目录写入，Plugin Manager 启动时扫描该目录。

**硬件通信层的核心是通用协议解析**——内置硬件积木和用户插件都在 runtime.js 中调用同一套 `send()` 函数。`send()` 的底层实现根据用户选择的连接方式（Serial/Bluetooth/WebSocket）自动路由到对应的硬件桥接器。

**硬件固件**在 docs/firmware/ 目录下提供参考实现。MVP 阶段只需 micro:bit 和 Arduino 两个固件，ESP32 可延后。固件代码应尽量精简——只有一个任务：在串口上收发 JSON 指令。不要试图在固件中做业务逻辑，业务逻辑在插件的 blocks.js 中。

---

[质量修订] 已自动修正：Agnes 全模态引擎替换三 Provider 架构；新增 1.5 商业模式（免费增值 + Credits 充值）；新增 3.7 AI 素材生成模块（角色/背景/视频）；新增桌面 Tauri 架构替换 Web；插件存储从 IndexedDB 改为文件系统 `~/.scratchmind/plugins/`；积木引擎从 scratch-gui 换为 TurboWarp（60FPS + JIT 编译）；硬件通信从 Web Serial 换为原生 serialport crate；更新全部已知未知项和验收剧本。
